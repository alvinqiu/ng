import { UserListServiceDirective } from './user-list-service.directive';

describe('UserListServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new UserListServiceDirective();
    expect(directive).toBeTruthy();
  });
});
